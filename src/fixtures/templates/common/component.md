import { {{ Entity }}Props } from './{{ Entity }}Types';

/**
 * {{ Entity }} component
 */
export function {{ Entity }}({ }: {{ Entity }}Props) {
  return (
    <div className="{{ entity }}-container">
      <h1>{{ Entity }}</h1>
      {/* TODO: Implement component */}
    </div>
  );
}
