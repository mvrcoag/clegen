import { StyleSheet } from 'react-native';

/**
 * {{ Entity }} Styles for React Native
 */
export const {{ entity }}Styles = StyleSheet.create({
  container: {
    padding: 16,
  },
  header: {
    marginBottom: 16,
  },
  content: {
    // TODO: Add styles
  },
  footer: {
    marginTop: 16,
  },
});
